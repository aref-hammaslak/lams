version: '3.8'

services:
  frontend:
    image: ${DOCKER_IMAGE_TAG}
    restart: always
    networks:
      - lams-net
    ports:
      - 127.0.0.1:${LAMS_FRONT_PORT}:80

networks:
  lams-net:
    name: lams-net
    driver: bridge
    external: true
